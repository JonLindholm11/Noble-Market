apiVersion: v1
kind: ConfigMap
metadata:
  name: noble-market-config
  namespace: noble-market
data:
  DATABASE_URL: postgresql://postgres:postgres@postgres-service:5432/noble_market
  NODE_ENV: production
  PORT: "3000"
  # TODO: Move to Kubernetes Secret for production deployment
  # Secrets provide base64 encoding and better access controls
  JWT_SECRET: supper-secret-key
